// jest.setup.js
// This file can be used for global test setup
// For example, setting up environment variables for tests

// If you are using a test database, you might initialize or clean it here
// import { PrismaClient } from '@prisma/client';
// const prisma = new PrismaClient();

// afterEach(async () => {
//   // Example: Clean up database after each test
//   // await prisma.product.deleteMany({}); // Be careful with this in a real app
// });

// beforeAll(async () => {
  // Example: connect to test database or run migrations
// });

// afterAll(async () => {
  // Example: disconnect from test database
  // await prisma.$disconnect();
// }); 